name: Keep Alive

on:
  schedule:
    - cron: '0 12 * * 2'  # toda terça-feira ao meio-dia UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s https://lavouras-ro.streamlit.app/ > /dev/null
